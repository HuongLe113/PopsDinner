@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="Item"
       asp-action="Index"
       class="dropdown-toggle" data-toggle="dropdown">Sản phẩm<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Item" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Item" asp-action="List" asp-route-Category="">Xem toàn bộ sản phẩm</a>
        </li>
    </ul>
</li>